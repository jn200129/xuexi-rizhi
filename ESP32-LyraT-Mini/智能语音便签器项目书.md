# 基于 ESP32-LyraT-Mini 的智能语音便签器 · 项目设计书

> 项目负责人：江楠
> 主控平台：ESP32-LyraT-Mini V1.2（乐鑫官方音频开发板）
> 开发框架：ESP-IDF v5.3.1 + ESP-ADF v2.7
> 立项日期：2026-07-03
> 计划周期：6 周（含验收）

---

## 0. 项目一句话概括

**「一台离线可用的语音便签器：喊唤醒词就能录下想法，按键回放，SD 卡本地存储，Wi-Fi 连网时自动同步到自建服务器。」**

关键词：离线唤醒 · VAD 自动断句 · AMR 压缩 · SD 存储 · 按键回放 · 云同步 · 电池续航

---

## 1. 项目立项

### 1.1 背景与选题

灵感/备忘随时会冒出来，掏手机打字/录音门槛都比想象中高——尤其在走路、开车、做饭时。桌面式语音便签器（一按就录、说完就完、要用时回放）能真正做到「零操作成本」的想法收集，是被小众硬件市场（Rocketbook Beacon、Bushnell Notebot）反复验证过的形态。

现有智能音箱不适合这个场景：① 依赖云识别 → 断网即残废、隐私不可控；② 唤醒后必须"回答"某个问题 → 不支持"我只是想记一句话"的自由留言模式。

**本项目要做的差异化**：所有语音数据**默认只存在本地 SD 卡**（隐私优先），云同步是可选功能；离线 WakeNet 唤醒 + VAD 自动收尾，全程双手无需接触设备。

### 1.2 选平台：为什么是 LyraT-Mini？

| 需求 | LyraT-Mini 的对应能力 |
|---|---|
| 离线语音唤醒 | ESP32 + 8 MB PSRAM 足够跑 WakeNet 唤醒词模型 |
| 录音 + 压缩存 SD | 板载麦克风（ES7243）+ SD 卡槽 + ADF 内置 AMR-NB 编码器 |
| 按键交互（录/放/删/切） | 板载 6 个功能键（Rec / Mode / Play / Set / Vol±） |
| 播放回听 | ES8311 单声道 Codec + 3.5 mm 耳机 + 4Ω 3W 喇叭输出 |
| 便携/续航 | AP5056 锂电池充电管理，支持 3.7V 电池直接供电 |
| 云同步 | 集成 Wi-Fi + TCP/HTTP/MQTT 栈（ESP-IDF 原生支持） |
| 单板成本 | 乐鑫官方板，约 200 元，量产原型直接可用 |

**结论：完美贴合，不需要任何额外硬件即可完成 MVP**（后续可选加 OLED、WS2812 状态灯做视觉反馈）。

### 1.3 交付预期（MVP）

一台带外壳的成品便签器，用户拿到手能做的事：
1. 上电听到"叮"提示音 → 系统就绪；
2. 说"嗨乐鑫"（唤醒词）→ LED 变绿 → 开始录音，说完停顿 1.5 秒自动结束；
3. 按 **Play** 键：从最新一条便签开始按顺序播放；
4. 按 **Mode** 键：切换到"上一条/下一条"浏览；
5. 长按 **Set** 键：删除当前便签；
6. 有 Wi-Fi 时后台自动上传新便签到自建 HTTP 服务器（POST AMR 文件）；
7. 锂电池续航 ≥ 8 小时（待唤醒 + 偶尔录放）。

---

## 2. 需求分析与功能指标

### 2.1 功能需求（MoSCoW 优先级）

| 分类 | 需求 | 优先级 |
|---|---|---|
| Must | 上电初始化 → 待唤醒 → 录音 → 存 SD → 按键回放 | P0 |
| Must | 唤醒词误触发率 < 1 次/小时（近场安静环境） | P0 |
| Must | 断电不丢数据（掉电前完成 fclose） | P0 |
| Must | 电池电量指示（LED 呼吸/串口日志） | P0 |
| Should | Wi-Fi 上传到 HTTP 服务器 | P1 |
| Should | VAD 自动检测录音结束（尾静音判定） | P1 |
| Should | 按键删除便签、切换便签 | P1 |
| Could | 唤醒后录制"提示音 → 录音"的反馈音效 | P2 |
| Could | 板载 6 键 + Wi-Fi 长按配网（SmartConfig / BLE） | P2 |
| Won't | 语音转文字（本 MVP 不做，云端交由服务器侧处理） | — |
| Won't | 联动其他 IoT 设备 | — |

### 2.2 性能指标

| 指标 | 目标值 | 依据 |
|---|---|---|
| 采样率 | 16 kHz / 16-bit / 单声道 | WakeNet 要求 16 kHz，录音复用同一路 |
| 编码格式 | AMR-NB 12.2 kbps | 语音压缩率高（1 分钟 ≈ 92 KB），ADF 内置编码器 |
| 单条便签最长 | 60 秒 | 硬性截断，避免误触发无限录音 |
| SD 卡容量下限 | 支持 ≥ 4 GB（FAT32） | 4 GB 可存约 12 小时便签 |
| 唤醒响应延时 | ≤ 500 ms | 从说完唤醒词到"叮"提示音 |
| 录音起停延时 | ≤ 300 ms | 用户感知的"跟手性" |
| 从待唤醒到播放开始 | ≤ 800 ms | 按键触发到出声 |
| 静态功耗 | ≤ 60 mA @ 3.7V（待唤醒） | Wi-Fi 关闭 + Modem-Sleep + ADC 常开 |
| 一次充满续航 | ≥ 8 小时（待唤醒 + 每小时 5 分钟录放） | 1000 mAh 电池 @ 平均 100 mA |
| 上传吞吐 | ≥ 500 kbps（Wi-Fi 802.11n） | 一条 60 秒便签 ≤ 2 秒上传 |
| 崩溃恢复 | 看门狗触发 → 3 秒内重启并恢复到待唤醒 | esp_task_wdt + coredump 分区 |

### 2.3 用户交互矩阵

| 状态 | Rec | Mode | Play | Set | Vol− | Vol+ |
|---|---|---|---|---|---|---|
| 待唤醒 | 手动进入录音 | Wi-Fi 开关 | 播放最新 | 长按删最新 | 音量− | 音量+ |
| 录音中 | 手动结束 | — | — | — | — | — |
| 播放中 | — | 上一条 | 暂停/继续 | 下一条 | 音量− | 音量+ |
| 配网中 | 取消 | — | — | 长按恢复出厂 | — | — |

**LED 语义**（未来加 WS2812 后细化）：
- 绿色常亮：待唤醒；红色常亮：录音中；蓝色呼吸：播放中；黄色闪烁：Wi-Fi 上传；红色三闪：错误。

---

## 3. 硬件方案

### 3.1 硬件框图

```
              ┌─────────────────────────────────────────────┐
              │           ESP32-WROVER-E                    │
              │  (双核 240 MHz · 4 MB Flash · 8 MB PSRAM)   │
              │                                             │
   麦克风 ──▶ ES7243 ──I2S1──▶ RX 环形缓冲 ──▶ WakeNet     │
                                              └─▶ VAD ──▶ AMR 编码 ──▶ SD 写
                                                                     │
   喇叭 ◀── 功放 ◀── ES8311 ◀─I2S0─ AMR 解码 ◀─ SD 读 ◀──┘
              │
              ├── I2C ──▶ ES8311/ES7243 配置
              ├── GPIO ──▶ 6 键扫描 (ADC 分压)
              ├── SPI ──▶ MicroSD (1-bit SDIO)
              └── Wi-Fi ──▶ HTTP POST 上传服务
```

### 3.2 引脚映射（LyraT-Mini V1.2）

| 功能 | ESP32 GPIO | 说明 |
|---|---|---|
| I2S0 BCK / WS / DOUT | GPIO5 / 25 / 26 | 送 ES8311 播放 |
| I2S1 BCK / WS / DIN | GPIO32 / 33 / 35 | 收 ES7243 采集 |
| I2C SCL / SDA | GPIO23 / 18 | Codec 配置总线 |
| SD DAT0 / CLK / CMD | GPIO2 / 14 / 15 | 1-bit SDIO |
| KEY_ADC | GPIO36 (SENSOR_VP) | 6 键电阻分压走单路 ADC |
| PA_EN | GPIO21 | 功放使能 |
| BAT_ADC | GPIO39 (SENSOR_VN) | 电池电压检测 |

> 所有引脚由 ADF 的 `audio_board_init()` 一次性配置好，应用层不需要手动配 pinctrl。

### 3.3 外壳与配件（MVP 阶段）

- 3D 打印外壳（Bambu Studio 设计，PLA，白色）：暴露 6 键 + 麦克风孔 + 喇叭出音口 + 充电口 + SD 卡口；
- 3.7V 1000 mAh 锂电池（京东 3 元/块，MX1.25 接口）；
- 4Ω 3W 圆形扬声器（直径 40 mm）；
- SanDisk 32 GB Class 10 MicroSD。

---

## 4. 软件架构

### 4.1 总体架构（分层）

```
┌──────────────────────────────────────────────┐
│  应用层：状态机（IDLE→WAKE→REC→PLAY→UPLOAD） │
├──────────────────────────────────────────────┤
│  业务组件                                    │
│  ├── 唤醒管理（WakeNet + VAD）              │
│  ├── 录音管道（i2s_stream → amr_encoder →  │
│  │             fatfs_stream）               │
│  ├── 播放管道（fatfs_stream → amr_decoder → │
│  │             i2s_stream）                 │
│  ├── 便签管理（元数据 JSON + 文件枚举）    │
│  ├── 按键管理（button_svc + 事件分发）      │
│  └── 云同步（Wi-Fi + HTTP POST + 队列）    │
├──────────────────────────────────────────────┤
│  ESP-ADF（audio_pipeline / audio_element / │
│           esp-sr / audio_board）            │
├──────────────────────────────────────────────┤
│  ESP-IDF v5.3.1（FreeRTOS + LwIP + FATFS）  │
└──────────────────────────────────────────────┘
```

### 4.2 核心状态机

```
      电源 ON
        │
        ▼
┌────────────┐    唤醒词/Rec键     ┌────────────┐
│   IDLE     │────────────────────▶│ RECORDING  │
│ (待唤醒)   │                    │ (录音中)   │
│ WakeNet on │                    │ VAD on     │
└────────────┘                    └─────┬──────┘
    ▲   ▲                                │VAD 静音 3s
    │   │Play键                          │/Rec键
    │   │                                ▼
    │   │  ┌────────────┐   Set长按  ┌────────────┐
    │   └──│  PLAYING   │◀───────────│  SAVING    │
    │      │(播放/浏览) │            │(存 SD +    │
    │      └────────────┘            │ 加入队列)  │
    │            │Stop/播完           └────────────┘
    │            ▼                          │
    │      ┌────────────┐  Wi-Fi 可用      │
    └──────│ UPLOADING  │◀─────────────────┘
           │(HTTP POST) │
           └────────────┘
                │上传完/失败
                ▼
             回 IDLE
```

**FreeRTOS 任务划分**（核心亲和性）：
| 任务 | 核心 | 优先级 | 栈 | 职责 |
|---|---|---|---|---|
| `app_main` (状态机) | 任意 | 5 | 4 KB | 事件总线，切状态 |
| `wakenet_task` | 1 | 8 | 8 KB | 音频 buffer 送 WakeNet |
| `pipeline_task` | 1 | 7 | 4 KB | ADF pipeline 事件泵 |
| `button_task` | 任意 | 4 | 2 KB | 按键去抖 + 长短按判定 |
| `upload_task` | 0 | 3 | 6 KB | Wi-Fi + HTTP，可睡眠 |

### 4.3 数据结构

**便签元数据（`/notes/index.json`）**：
```json
{
  "version": 1,
  "notes": [
    {"id": "n_1751508000", "path": "/notes/n_1751508000.amr",
     "duration_ms": 12300, "created": 1751508000, "uploaded": false}
  ],
  "next_id": 12
}
```

**SD 卡目录结构**：
```
/notes/
  index.json
  n_1751508000.amr   ← Unix 时间戳作 ID
  n_1751508735.amr
  ...
/log/
  system.log         ← 循环日志（每 1MB 一轮）
/model/
  wn9_hiesp/         ← WakeNet 模型（也可编进 partition）
```

---

## 5. 分周迭代计划

| 周 | 里程碑 | 交付物 | 验收 |
|---|---|---|---|
| **W1** | 环境搭建 + 硬件跑通 | 板子能烧 `play_mp3_control` 出声、录音存 SD 能听 | idf.py flash monitor 无红字，SD 里能听到自己录的 wav |
| **W2** | 状态机骨架 + 按键 | 6 键都能触发状态切换（用串口 log 模拟状态） | 6 键功能表完全响应 |
| **W3** | 唤醒 + VAD + 完整录音流水线 | 说唤醒词 → 提示音 → 录音 → SD 保存 | 连续录 10 条不丢文件 |
| **W4** | 便签管理 + 播放/删除/浏览 | Mode/Play/Set 完整交互，index.json 一致 | 断电重启后列表完整 |
| **W5** | Wi-Fi + 云同步 + 电池管理 | 有网时后台上传、无网时排队，电量指示 | 断网测试 + 8 小时续航实测 |
| **W6** | 外壳 + 整机联调 + 验收 | 装壳成品、README、演示视频 | 用户测试 3 天无 Bug 通过 |

**每周产出**：commit + 一段 30 秒功能演示视频 + 一页周报（放在 `docs/weekly/`）。

---

## 6. 关键技术点解析

### 6.1 WakeNet + VAD 的协同

WakeNet 是持续检测（每帧滑窗），VAD 是"人声有/无"的粗判。二者用法：

- **待唤醒态**：只跑 WakeNet，检测到唤醒词 → 转录音态；
- **录音态**：关闭 WakeNet（省 CPU）、开 VAD；连续 1.5 秒判无人声 → 结束录音；
- **超时保护**：无论 VAD 结果，60 秒硬截止（防止风扇噪声被判成人声无限录下去）。

ESP-ADF 里 `esp_sr` 组件的用法：
```c
srmodel_list_t *models = esp_srmodel_init("model");
esp_wn_iface_t *wakenet = (esp_wn_iface_t *)esp_wn_handle_from_name(
    esp_srmodel_filter(models, ESP_WN_PREFIX, NULL));
model_iface_data_t *model = wakenet->create(
    esp_srmodel_filter(models, ESP_WN_PREFIX, NULL), DET_MODE_2CH_90);
// 每帧送数据：
int r = wakenet->detect(model, i2s_buffer);
if (r > 0) { /* 唤醒词命中 */ }
```

### 6.2 ADF pipeline 组装（录音路径）

```c
audio_pipeline_handle_t rec_pipe = audio_pipeline_init(&pcfg);
audio_element_handle_t i2s_reader = i2s_stream_init(&i2s_read_cfg);
audio_element_handle_t amr_enc    = amr_encoder_init(&amr_cfg);
audio_element_handle_t fs_writer  = fatfs_stream_init(&fs_write_cfg);
audio_pipeline_register(rec_pipe, i2s_reader, "i2s");
audio_pipeline_register(rec_pipe, amr_enc,    "enc");
audio_pipeline_register(rec_pipe, fs_writer,  "fs");
audio_pipeline_link(rec_pipe, (const char*[]){"i2s","enc","fs"}, 3);
audio_element_set_uri(fs_writer, note_path);   // "/sdcard/notes/n_1751508000.amr"
audio_pipeline_run(rec_pipe);
```

播放路径把 3 个元素反过来（fatfs_stream → amr_decoder → i2s_stream）+ 换 I2S 方向即可。

### 6.3 SD 卡"掉电不丢数据"

问题：如果录音中拔电，最后几秒/整个文件都会损坏。对策：
1. 每写完一帧 AMR（20 ms 音频 = 32 字节）后**不立刻 sync**，但每 5 秒 `f_sync()` 一次；
2. 元数据 `index.json` 采用**双缓冲写**：先写 `index.json.tmp` → sync → rename；
3. 加一个 GPIO 检测充电断开 → 立刻 flush + fclose；
4. 启动时扫描 `/notes/` 里没在 `index.json` 里的孤儿 .amr → 自动恢复。

### 6.4 电池续航优化

| 场景 | 平均电流 | 手段 |
|---|---|---|
| 待唤醒 | 60 mA | Wi-Fi 关；CPU DFS 降至 80 MHz；ES7243 常开；WakeNet 常跑 |
| 录音中 | 90 mA | 同上 + SD 写 |
| 播放中 | 130 mA | + 功放 + Codec 播放 |
| Wi-Fi 上传中 | 240 mA（峰值 500 mA） | 集中批量上传（一小时一次）|

`esp_pm_configure()` 打开 DFS（Dynamic Frequency Scaling）+ Light Sleep（保持外设唤醒源）。1000 mAh 电池：60 mA × 8h = 480 mAh，剩 500 mAh 余量给录放和上传峰值——**理论续航 ≥ 10 小时**。

---

## 7. 测试计划

### 7.1 单元测试

| 模块 | 用例 | 通过条件 |
|---|---|---|
| SD 文件 IO | 连续写 100 条 30 秒 AMR | 无错误、index.json 一致 |
| 按键去抖 | 100 次快速按 Play | 只触发 100 次事件（不重复不漏） |
| VAD | 连续录 5 分钟静音 | 5 分钟内应正确结束（不无限录） |
| WakeNet | 100 次说唤醒词 | 命中 ≥ 95 次；连续说非唤醒词 100 次误触发 ≤ 2 次 |
| HTTP 上传 | 断网上传 → 联网重传 | 所有便签都上传成功，无重复 |

### 7.2 集成测试（场景）

- **场景 A：办公室连续 8 小时使用**——记 20 条便签，全部可播、可上传、电池剩余 ≥ 20%；
- **场景 B：走路时手持**——WakeNet 命中率不低于办公室场景 -20%；
- **场景 C：拔电测试**——录音中拔电 3 次、播放中拔电 3 次，重启后所有文件仍能播放；
- **场景 D：SD 卡快满**——SD 剩余 &lt; 100 MB 时提示、拒绝新录音、允许播放已有的。

### 7.3 环境测试

- 温度：0 °C ~ 40 °C 各录 5 条便签；
- Wi-Fi：家用 2.4 GHz / 5 GHz / 公司企业 Wi-Fi 三种网络各测；
- 供电：USB 直供 / 电池 / 边充边用三种模式各测。

---

## 8. 风险与备选方案

| 风险 | 概率 | 影响 | 备选方案 |
|---|---|---|---|
| WakeNet 官方唤醒词误触发多 | 中 | 高 | 换成"嗨小江"自训练模型（ESP-SR 支持自训练） |
| ES7243 单麦拾音距离不够 | 中 | 中 | 用户手册指导：正对麦克风 30 cm 内说话 |
| SD 卡兼容性差异 | 低 | 中 | 只推荐 Class 10 品牌卡，README 列出兼容清单 |
| ESP32 经典款 Wi-Fi + BT 共存不稳 | 低 | 低 | MVP 阶段先只做 Wi-Fi，蓝牙留到 P2 |
| 唤醒词冲突（家里恰好有人说） | 中 | 低 | 加"唤醒后需 500 ms 静音再进入录音"防误触 |
| 3D 打印外壳散热差 | 低 | 低 | 外壳开散热槽 + Wi-Fi 上传后立刻 sleep |

---

## 9. 交付物清单

- [ ] 项目源码（GitHub 仓库 + Tag v1.0）
- [ ] 硬件：装壳成品一台 + 备用 PCB 一块
- [ ] 文档：本项目书（本文）+ README + 用户使用手册（一页 A4）
- [ ] 服务器：一份最小 HTTP 接收脚本（Flask，接收 AMR 存文件）
- [ ] 视频：3 分钟功能演示
- [ ] 周报：6 篇（每周一篇，含遇到的问题和解法）

---

## 10. 学习收获目标

完成本项目后，我应该：
1. 独立跑通 ESP-IDF + ESP-ADF 的开发/调试流程；
2. 掌握 FreeRTOS 多任务、事件组、队列在实时音频场景的用法；
3. 理解 ADF pipeline / element / stream 的组装范式，能"想到什么就串什么"；
4. 掌握 WakeNet / VAD 的实际用法与调参（阈值、灵敏度、模型选择）；
5. 掌握 Wi-Fi + HTTP Client + 掉电保护的工程实践；
6. 会用 `idf.py` 全套命令（build/flash/monitor/menuconfig/size/coredump）；
7. 具备把 ESP32 音频类项目从想法带到成品的能力。

---

## 附录 A：环境搭建速查

```cmd
:: 每次开新终端都要跑（顺序不能变）
set IDF_TOOLS_PATH=D:\esp\espressif
set IDF_PATH=D:\esp\esp-adf\esp-idf
D:\esp\esp-adf\export.bat

:: 验证
idf.py --version    :: 应输出 ESP-IDF v5.3.1
```

## 附录 B：参考资料

- ESP-ADF 官方文档：https://docs.espressif.com/projects/esp-adf/zh_CN/latest/
- ESP-IDF 编程指南：https://docs.espressif.com/projects/esp-idf/zh_CN/v5.3.1/esp32/
- ESP-SR（语音识别）：https://github.com/espressif/esp-sr
- LyraT-Mini 板子入门笔记：见同 repo `ESP32-LyraT-Mini音频开发入门.html`
- 本项目 GitHub：https://github.com/jn200129/xuexi-rizhi

---

*立项人：江楠 · 立项日期：2026-07-03 · 版本：v1.0（初稿）*
